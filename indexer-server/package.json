{
  "name": "indexer-registry-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Off-chain indexer node for the IndexerRegistry protocol on Base (job #259). Watches CLAWD Transfer events, serves Clickable Questions at $0.10 each, settles on-chain via settleQuery.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "start": "tsx src/server.ts",
    "query": "tsx scripts/example-consumer.ts",
    "register": "tsx scripts/register.ts"
  },
  "dependencies": {
    "@hono/node-server": "^1.13.7",
    "dotenv": "^16.4.7",
    "hono": "^4.6.14",
    "tsx": "^4.19.2",
    "viem": "^2.21.55"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
